setenv mmc_rootpart 2
part uuid mmc ${mmc_bootdev}:${mmc_rootpart} uuid
setenv loadkernel fatload mmc \$mmc_bootdev \$kernel_addr_r uImage
setenv bootargs root=PARTUUID=${uuid} rootwait bootdev=${mmc_bootdev}
setenv uenvcmd run loadkernel \&\& bootm \$kernel_addr_r - \$fdtcontroladdr
run uenvcmd
